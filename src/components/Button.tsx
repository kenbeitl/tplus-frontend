interface ButtonProps {
    iconBefore: React.ReactNode;
    iconAfter?: React.ReactNode;
    label: string;
}

export default function Button({}:ButtonProps): React.ReactNode {
    return (
        <></>
    )
}